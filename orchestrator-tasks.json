{
  "ceoTask": "Gallery Soma overnight automation run: assign one focused backend task and one focused frontend task that address concrete user-visible or test-visible defects from current repo context, plus one dedicated swe3 testing/validation task (prefer adding or improving tests that verify swe1/swe2 outcomes; if no direct gap exists, do testing-focused hardening). Each task must be scoped to a single target file and include a measurable expected outcome (what behavior should change or what check should pass). Avoid generic cleanup-only tasks."
}
